import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.geoayto',
  appName: 'geoayto',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
